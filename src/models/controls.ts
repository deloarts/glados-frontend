export interface BoughtItemControls {
  changelog: boolean;
  rainbow: boolean;
  textOnly: boolean;
  fixedHeight: boolean;
  unclutter: boolean;
  requestView: boolean;
  lockCols: boolean;
}

export interface AvailableOption {
  text: string;
  value: string;
}
