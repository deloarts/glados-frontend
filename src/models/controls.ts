export interface BoughtItemControls {
  changelog: boolean;
  rainbow: boolean;
  fixedHeight: boolean;
  unclutter: boolean;
  lockCols: boolean;
}

export interface AvailableOption {
  text: string;
  value: string;
}
